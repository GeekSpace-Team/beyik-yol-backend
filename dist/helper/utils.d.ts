export declare const isNullValue: (value: any) => boolean;
export declare function randomIntFromInterval(min: number, max: number): number;
export declare function calcDate(date1: any, date2: any): {
    total_days: number;
    total_weeks: number;
    total_hours: number;
    total_minutes: number;
    total_seconds: number;
    result: string;
};
